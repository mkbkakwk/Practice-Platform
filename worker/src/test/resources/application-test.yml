spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:worker-schema.sql
      data-locations: classpath:worker-data.sql
  rabbitmq:
    listener:
      simple:
        auto-startup: false

oj:
  judge:
    workspace: ${JUDGE_WORKSPACE:/tmp/practice-platform-test-judge}
  rabbitmq:
    exchange: ${OJ_RABBITMQ_EXCHANGE:oj.test.judge}
    routing-key: ${OJ_RABBITMQ_ROUTING_KEY:oj.test.judge.submit}
    queue: ${OJ_RABBITMQ_QUEUE:oj.test.judge.queue}

logging:
  level:
    com.oj: WARN
